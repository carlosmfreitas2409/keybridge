import {
  name as NAME,
  displayName as TITLE,
  version as VERSION,
} from '../../../package.json';

export const APP = {
  NAME,
  TITLE,
  VERSION,

  MAIN: {
    WINDOW: {
      WIDTH: 750,
      HEIGHT: 96,
    },
  },
};
